Every time you create a project to study something, such as making an HTML page or a command line game with Node.js or anything, 
you’ll create a repository, clone it in your machine, work on it using branches, and make small commits to send to GitHub.

git config --global user.name="aamir00"
git config --global user.email="amairkhan.college@gmail.com"

git config -l
git config user.email

git help commit
-----------------------------------------------------------------------
git init
git add .
git commit -m "Commit message" //like saving the files

git log
git status 
git diff //difference between the working copy and the index
git diff --staged
git rm sample_3 //removes the 3rd file
git mv sample sample_1

git commit -am 
git checkout -- index.html // to reset the index.html
git checkout 01e7dba -- index.html

git checkout -b task_name
git diff master
git merge our_branch_name
git checkout master

get reset HEAD index.html // to remove index.html from the stagging area
git remote add git-tutorial https://github.com/aamir00/git-notes.git

git merge

git clone git@github.com:our-username/learning-html.git



--------------------------------------------------------------------------
sworking copy > staging > repository
abc
def
